[package]
name = "pool-v2"
version.workspace = true
edition.workspace = true
license.workspace = true
# Test fixture, never deployed. See test-fixtures/registry-v2/Cargo.toml.
publish = false

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
